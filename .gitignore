/target
**/*.png.new
**/*.actual.png

# Local Claude guidance stays out of the repo.
CLAUDE.local.md

# Headless example output (the `gallery` and `looks` showcases).
/gallery/
/looks/
\n# mdBook build output\nbook/book/\n